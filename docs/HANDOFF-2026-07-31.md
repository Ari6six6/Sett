# Handoff — end of 2026-07-31

Read this first tomorrow. `docs/MORNING-2026-07-31.md` is what was *predicted*
before the day started and is deliberately unedited; this is what happened.

## Run this first

```sh
cd /home/michael/SETT-repo
./sett selftest --fast     # 53s — are the gates still real?
./sett blind               # do the instruments notice when the world breaks?
./sett rot                 # does every path and verb still resolve?
```

All three should exit 0. If they do, every number below is still true.

---

## The one-line state

**The apparatus is sound and now audits itself. The security capability is
real and measured at n=10. Three sealed predictions are open and need one
GPU hour.**

## What is settled

### `stix-graph-12` at n=10 — the day's finding

**10, 12, 10, 11, 11, 11, 9, 11, 12, 12 — mean 10.90, spread 9–12** over a
25 843-object STIX bundle with 21 025 relationships to resolve.

**Four of twelve points (33%) were in the wrong bucket at n=3**, errors running
both ways:

| point | n=3 | n=10 |
|---|---|---|
| 4, 6 | NOISY | **10/10 SOLID** |
| 7, 8 | SOLID | **9/10 NOISY** |

Points 6 and 10 both read `2/3` at three reps. Point 6 is a 100% point. Point
10 is `6/10` — the real coin flip. **From inside a 3-rep run they were
indistinguishable.**

### The other gyms (n=3, box #4)

| gym | reps | mean |
|---|---|---|
| `code-10` | 9, 7, 9 | 8.33/10 |
| `code-sett-8` | 8, 7, 7 | 7.33/8 |
| `debug-7` | 5, 5, 3 | 4.33/7 |
| `index-6` (new) | 4, 5, 3 | 4.00/6 |

Authoring 83%, repairing 62% — same model, box, hour and checks. `debug-7`
produced the day's only ERRORs, both `pi exit 124`: **repair does not just fail,
it stalls.**

### Eleven defects, ten of them the operator's

Three of the eleven are one pattern, and it is the most useful thing learned:

> **An instrument narrows to the world that existed when it was written, and
> keeps printing the same reassuring word while it does.**

- `rot` audited literal `/home/...`; specs went portable; coverage fell 31 → 13
  and it still said `clean`
- `lint`'s schema check was hardcoded to two formats; a third appeared, lint
  said 0, and the point it missed scored 0/3
- gate C lost its parser and reported `0/0 … 0 FAKE`, **exit 0** — which
  selftest called clean on every spec, because `0 == 0`

None was found by reading code. Each was found by breaking the world on
purpose. That method is now `sett blind`, and its first version was itself
blind twice.

## What is OPEN — three sealed predictions, one GPU hour

The box was killed mid-experiment. Arm A rep 1 of the law A/B scored 8/10 and
that is all; **one rep of one arm settles nothing** and it is recorded as open
rather than salvaged.

| # | prediction | cost |
|---|---|---|
| 1 | **`code-10` p9 goes 0/3 → 3/3** with `SEAT.md` attached. It is dead only because "jq is NOT installed" was never in the prompt. This is the sharpest untested claim in the repo | ~25 min |
| 2 | **`index-6` p5 moves off 0/3** now that all three table schemas are stated | ~8 min |
| 3 | **`stix-graph-12` at ctx 65536** on one box separates "n=3 under-measured" from "the bigger box helped" — retires a confound I created | ~18 min |

```sh
sett birth "<ssh string>" --model glm-q4
sett doctor
sett ab code-10 3 - "$PWD/SEAT.md"      # 1 — do this one first
sett runs index-6 3                      # 2
sett runs stix-graph-12 3                # 3, after dropping ctx to 65536
sett kill
```

## New today

`sett blind` · `sett prompts` · `sett behave --session` · `sett cast` (removed —
see below) · `corpus/fetch.sh` · `patches/km-prepull.patch` · `index-6` gym ·
`GLOSSARY.md` · portable specs (`$SETT` / `$CORPUS`, zero hardcoded home paths) ·
`sett score` as a real computed ledger with `spec@` hashing

**The GPU module is fixed and proven live.** HF throttles per connection;
llama.cpp downloads single-stream. 16 parallel range requests took 18 GB from
~50 minutes to ~5, sha256-verified against HF's own etag. `km` is not under
version control and reinstalls over itself, so the fix lives in
`patches/km-prepull.patch` — **reapply it after any KM update.**

## Two things deliberately removed

- **`sett cast`** — eight "spell" commands. Removed as features where polish was
  asked for. Their receipts were also `a = a`: same data, same predicate, same
  author, so agreement was guaranteed before they ran.
- The **compounding claim** in the old README — that model-written utilities
  audited the next gym. The parser is hand-written and no artifact matches it.
  Unsupported; see defect 9.

## Housekeeping

- **The vast.ai instance must be destroyed in the console.** `sett kill` stops
  the server and tunnel, not the billing.
- 38+ commits pushed to `github.com/Ari6six6/Sett`, working tree clean.
- Vault `gold` verified INTACT — 15 132 files.
- `leak` clean across every run today.

## The honest summary

Yesterday's finding was that the model was never the ceiling — the instructions
were. Today sharpened it: **the instruments were not the ceiling either, until
they were tested against a world that had moved.** Three of them had gone blind
and every one of them was still printing the reassuring word.

The badger keeps one den and digs deeper.
